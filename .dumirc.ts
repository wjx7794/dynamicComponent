import { defineConfig } from 'dumi';

export default defineConfig({
  base: '/dynamicComponent/',
  publicPath: '/dynamicComponent/',
  outputPath: 'docs-dist',
  themeConfig: {
    name: 'Dynamic',
  },
});
